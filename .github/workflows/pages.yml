# Публикация на GitHub Pages.
#
# Pages «из ветки» умеет отдавать только корень или /docs, а собранный файл
# лежит в dist/. Поэтому публикуем через Actions: так каталог может быть любым
# и в репозитории не появляется второй копии файла на полмегабайта.
#
# Один раз в настройках репозитория: Settings → Pages → Source → GitHub Actions.
# Включить их самим workflow (configure-pages с enablement: true) не выходит:
# токену Actions не разрешено создавать Pages-сайт, попытка падает с
# «Resource not accessible by integration».
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - id: deployment
        uses: actions/deploy-pages@v4
